jobs:
- job: Skia_Desktop
  timeoutInMinutes: 90
  pool:
    vmImage: windows-2022

  steps:
  - template: templates/canary-updater.yml
  # - template: templates/gitversion.yml
  - template: templates/dotnet-install-windows.yml

  - task: MSBuild@1
    displayName: Build project
    retryCountOnTaskFailure: 3
    inputs:
      solution: $(build.sourcesdirectory)\SDKExamples\SDKExamples\SDKExamples.csproj
      msbuildArguments: /ds /m /r /p:TargetFramework=net9.0-desktop /p:UnoTargetFrameworkOverride=net9.0-desktop /p:Configuration=Release  /bl:$(build.artifactstagingdirectory)/build-desktop.binlog

  - task: CopyFiles@2
    displayName: Copy Skia output
    inputs:
      SourceFolder: $(build.sourcesdirectory)\SDKExamples\SDKExamples\bin\Release\net9.0-desktop
      Contents: '**/*.*'
      TargetFolder: $(build.artifactstagingdirectory)/bin

  - task: PublishBuildArtifacts@1
    condition: always()
    inputs:
      PathtoPublish: $(build.artifactstagingdirectory)
      ArtifactName: skia-desktop
      ArtifactType: Container